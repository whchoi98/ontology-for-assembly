# 클라이언트 (브라우저)에서 호출하는 API URL.
# 개발: http://localhost:8080
# production: https://<cloudfront-domain> (CloudFront → ALB → API)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# 서버 컴포넌트 (Server Action·SSR)에서 호출 - 동일 VPC 내부 ALB DNS.
# 미설정 시 NEXT_PUBLIC_API_BASE_URL을 사용.
INTERNAL_API_BASE_URL=
